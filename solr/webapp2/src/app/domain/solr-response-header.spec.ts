import { SolrResponseHeader } from './solr-response-header';

describe('SolrResponseHeader', () => {
  it('should create an instance', () => {
    expect(new SolrResponseHeader()).toBeTruthy();
  });
});
