{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "A name for the library"
    },
    "sha256": {
      "type": "string",
      "description": "The sha256 hash of the jar"
    },
    "sig": {
      "type": "string",
      "description": "the signature of the jar"
    },
    "repository": {
      "type": "string",
      "description": "the repository name"
    },
    "version": {
      "type": "string",
      "description": "version of he package"
    },
    "setup-commands": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Commands that are passed on to the config API to deploy this plugin"
    }
  },
  "required" : ["name","sha256"]

}