{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The sha256 hash of the file"
    },
    "sig": {
      "type": "string",
      "description": "the signature of the file"
    }
  },
  "required": ["id", "sig"]
}
